# AutoBlogger Frontend

React (Vite) UI for generating and browsing articles.

## Run

```bash
npm install
npm run dev
```

## Configuration

Optional `frontend/.env`:

```bash
VITE_API_BASE_URL=http://localhost:5001/api
```

